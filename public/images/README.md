# Images

Add Logan's portrait here as **`logan.jpg`** (portrait orientation, e.g. 800×1000).

The About page (`src/pages/about.astro`) shows a "Photo coming soon" placeholder
until this file exists — once added, swap the placeholder block for the `<img>`
(there's a clearly marked TODO in that file).
